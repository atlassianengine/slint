// Rust guideline compliant 2026-02-21

//! Git status, staging, line diffing, and commit controller.

use crate::AppWindow;

/// Installs the Git Diff controller onto the main Slint `AppWindow`.
pub fn install(_app: &AppWindow) {
    // Git diff callbacks go here
}
