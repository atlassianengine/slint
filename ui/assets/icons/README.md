# Retrospect Slint icons

These are local outline SVG assets from [Tabler Icons](https://tabler.io/icons),
used under its MIT licence. They are bundled with the Slint app and recoloured
at render time by `RetrospectIcon`; the app does not depend on a web icon font
or on the sibling TypeScript workspace at runtime.
